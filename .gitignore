node_modules/
package.json
package-lock.json
__pycache__/

favorites.json

# Extension build artifact — rebuild with extension/build.sh
voice-bridge.xpi

# AMO signing credentials — never commit
.amo-credentials
voice-bridge-src.zip
*.xpi

# CMake build artifacts — rebuild with plugins/<name>/build.sh
plugins/*/build/

# Python virtualenv
venv/
